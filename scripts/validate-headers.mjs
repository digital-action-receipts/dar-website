import {readFile} from "node:fs/promises";const h=JSON.parse(await readFile("security-headers.json","utf8"));const required=["Content-Security-Policy","X-Content-Type-Options","Referrer-Policy","Permissions-Policy"];for(const k of required)if(typeof h[k]!=="string"||!h[k])throw new Error(`Missing header: ${k}`);if(!h["Content-Security-Policy"].includes("frame-ancestors 'none'"))throw new Error("CSP must deny framing.");console.log("Security-header baseline validated.");
